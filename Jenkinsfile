// @Library('roboshop') _

// nodejs(
//     COMPONENT                 : 'cart',
//     LABEL                     : 'WORKSTATION'
// )

@Library('roboshop') _


pipeline {
  agent none
  stages {
    stage ('Example') {
      steps {
        // log.info 'Starting'
        script {
          nodejs.info 'Starting'
          nodejs.warning 'Nothing to do!'
        }
      }
    }
  }
}
